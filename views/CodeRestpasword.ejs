<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Code de réinitialisation</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 0;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
        }
        h1 {
            color: #333;
            margin-bottom: 30px;
            text-align: center;
        }
        .success-message {
            background-color: #d4edda;
            color: #155724;
            padding: 15px;
            border-radius: 4px;
            margin-bottom: 20px;
            text-align: center;
            border-left: 4px solid #28a745;
        }
        .email-info {
            background-color: #fff3cd;
            color: #856404;
            padding: 15px;
            border-radius: 4px;
            margin-bottom: 20px;
            text-align: center;
            border-left: 4px solid #ffc107;
        }
        .error-message {
            background-color: #f8d7da;
            color: #721c24;
            padding: 15px;
            border-radius: 4px;
            margin-bottom: 20px;
            text-align: center;
            border-left: 4px solid #dc3545;
        }
        form {
            background-color: white;
            padding: 40px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0,0,0,0.1);
            width: 100%;
            max-width: 400px;
        }
        div {
            margin-bottom: 20px;
        }
        label {
            display: block;
            margin-bottom: 8px;
            color: #555;
            font-weight: bold;
        }
        input {
            width: 100%;
            padding: 12px;
            border: 1px solid #ddd;
            border-radius: 4px;
            box-sizing: border-box;
            font-size: 16px;
            text-align: center;
            letter-spacing: 2px;
            font-weight: bold;
        }
        input:focus {
            outline: none;
            border-color: #4CAF50;
            box-shadow: 0 0 5px rgba(76,175,80,0.2);
        }
        button {
            background-color: #4CAF50;
            color: white;
            padding: 14px 20px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            width: 100%;
            font-size: 16px;
            transition: background-color 0.3s;
            margin-bottom: 10px;
        }
        button:hover {
            background-color: #45a049;
        }
        .back-link {
            text-align: center;
            margin-top: 20px;
        }
        .back-link a {
            color: #0066cc;
            text-decoration: none;
            font-size: 14px;
        }
        .back-link a:hover {
            text-decoration: underline;
        }
        .instructions {
            font-size: 14px;
            color: #666;
            margin-bottom: 20px;
            text-align: center;
            line-height: 1.5;
        }
    </style>
</head>
<body>
    <h1>🔑 Code de réinitialisation</h1>
    
    <% if (typeof message !== 'undefined') { %>
        <div class="success-message">
            ✅ <%= message %>
        </div>
    <% } %>
    
    <div class="email-info">
        📧 Code envoyé à : <strong><%= email %></strong>
    </div>
    
    <div class="instructions">
        Veuillez saisir le code de réinitialisation à 4 chiffres que vous avez reçu par email.
        <br><strong>Le code expire dans 1 heure.</strong>
    </div>
    
    <form action="/users/modifieMotpasseByCode" method="POST">
        <input type="hidden" name="email" value="<%= email %>">
        <div>
            <label for="code">🔢 Code de réinitialisation :</label>
            <input type="text" id="code" name="code" required placeholder="____" maxlength="4" pattern="[0-9]{4}">
        </div>
        <button type="submit">✅ Vérifier le code</button>
    </form>
        <form action="/users/oublieMotpasse" method="post" style="display:inline;">
            <input type="hidden" name="email" value="<%= email %>">
            <button type="submit" style="background:none;border:none;color:#0066cc;cursor:pointer;font-size:14px;padding:0;">← Nouvel envoi de code</button>
        </form>
    
    </div>
</body>
</html>